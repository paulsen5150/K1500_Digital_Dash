# Maintenance Notes

## code.py Change Log Rule

For every future change to `code.py`, add one short dated sentence to the
`# Change log:` block at the top of `code.py` before committing.

Use this format:

```python
# YYYY-MM-DD: Short description of what changed.
```

Keep entries concise and focused on behavior, hardware support, or diagnostics.
